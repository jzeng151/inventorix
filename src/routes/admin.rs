// Admin: GET /admin, POST /admin/users, PUT /admin/users/:id, POST /admin/users/:id/deactivate
// Access: admin role only — 403 for coordinator and sales_rep
// TODO: implement user management panel, deactivate (invalidates sessions immediately)
